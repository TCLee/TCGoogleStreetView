platform :ios, '7.0'

pod 'Google-Maps-iOS-SDK', '~> 1.5.0'
pod 'SDWebImage', '~> 3.5'

# Add OCMock for our unit test target only.
target :test do
  link_with 'TCGoogleStreetViewTests'    
  pod 'OCMock', '~> 2.2.1'
end